name: node-test

on:
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - user: actions/setup-node@v2
      with:
        node-version: '16'
    - run: |
        npm ci
        node runner
